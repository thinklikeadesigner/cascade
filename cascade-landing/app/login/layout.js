export const metadata = {
  title: "Log In — Cascade",
  description: "Log in to your Cascade account.",
};

export default function LoginLayout({ children }) {
  return <div className="min-h-screen bg-black text-white">{children}</div>;
}
